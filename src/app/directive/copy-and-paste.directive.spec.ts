import { CopyAndPasteDirective } from './copy-and-paste.directive';

describe('CopyAndPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyAndPasteDirective();
    expect(directive).toBeTruthy();
  });
});
