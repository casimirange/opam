import { ClientNameFilterPipe } from './client-name-filter.pipe';

describe('ClientNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
