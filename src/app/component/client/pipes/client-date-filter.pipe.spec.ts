import { ClientDateFilterPipe } from './client-date-filter.pipe';

describe('ClientDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
