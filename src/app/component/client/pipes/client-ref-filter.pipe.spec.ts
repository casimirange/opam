import { ClientRefFilterPipe } from './client-ref-filter.pipe';

describe('ClientRefFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientRefFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
