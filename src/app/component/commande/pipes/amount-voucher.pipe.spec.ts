import { AmountVoucherPipe } from './amount-voucher.pipe';

describe('AmountVoucherPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountVoucherPipe();
    expect(pipe).toBeTruthy();
  });
});
