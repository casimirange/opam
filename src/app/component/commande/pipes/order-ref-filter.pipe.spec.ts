import { OrderRefFilterPipe } from './order-ref-filter.pipe';

describe('OrderRefFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderRefFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
